<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="maven.com.lguplus.mybatis.mapper.MybatisMemberxmlMapper">
    <select id="findMemberAll" resultType="MybatisMemberDto">
        SELECT * FROM member;
    </select>
    <insert id="mybatisMemberDtoAdd" useGeneratedKeys="true" keyProperty="member_id">
        INSERT into member (loginname,username,password,age)
          VALUES (#{loginname}, #{username},#{password},#{age})
    </insert>
</mapper>
